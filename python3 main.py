
## Project Structure
- `webapp.py`: Main entry point for the Flask web application.
- `main.py`: Entry point for the CLI application.
- `config.py`: Configuration settings and path definitions.
- `database/`: Contains database interface and schema.
- `services/`: Contains business logic (attendance, calendar, weights).
- `uploads/`: Directory for storing uploaded files.
